VITE_API_URL=http://localhost:8081

# Optional: admin login for static/GitHub Pages builds (no backend)
# Set the same value as a GitHub Actions secret: VITE_ADMIN_PASSWORD
# VITE_ADMIN_PASSWORD=your-admin-password
